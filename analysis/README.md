# Analysis Scripts

## Setting up the virtual environment
1. install uv: https://docs.astral.sh/uv/getting-started/installation/
2. open the analysis' directory in the terminal
3. run `uv sync`
4. run `uv pip install -e .`